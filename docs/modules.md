# 模块说明与扩展点 / Modules

所有模块都在 `web/js/modules/`，通过 `registerModule()` 注册。导航、底栏、"关于"页的模块列表由注册表自动生成。

## 登录 `login`

- 三步：邮箱（域名白名单）→ 验证码（演示码）→ 首次资料（姓名、学院、届、地区、行业、可提供帮助、**学联身份与部门**）。
- **扩展点**：`auth.js` 的 `requestCode / verify` 换成真实 API；学联身份改为管理员审核字段。

## 首页 `home`

- 学联推送（学联公告、志愿者招募、我的任务）、快捷入口、最新分享、近期活动（按我的地区优先）、精选校友。
- **扩展点**：推送区可接入站内通知系统；快捷入口数组 `quick` 直接加项。

## 职业发展 `careers`（启动模块）

| Tab | 内容 | 数据 |
|---|---|---|
| 机会 & 内推 | 岗位卡片、行业/类型筛选、只看有内推、岗位详情（申请 / 请求内推 / 收藏）、发布岗位 | `jobs.json` |
| 求职时间线 | 各行业节奏，当前月份高亮，维护人 | `timelines.json` |
| 简历工坊 | 简历案例帖、请求修改、投递前自查、愿意帮看的校友 | `posts.json`（category=resume） |
| 求职小组 & Mock | 按行业的小组、下次 mock、加入、发起 | `groups.json` |
| 行业研究 | 帖子流、分类筛选、影响力榜、发帖 | `posts.json`、`users.points` |

帖子详情 `#/careers/post/:id`。`YL.careers.postCard / postForm / requireLogin` 供其他模块复用。

- **扩展点**：评论系统（目前只有计数）、内推请求的真实流转、影响力算法。

## 活动 `events`

- 地区 / 类型筛选、往期开关、RSVP、详情（组织者、名额进度、所属社群、学联看板入口）。
- "我想办活动"按钮打开向导（见学联后台）。
- **扩展点**：日历导出、签到、提醒推送。

## 社群 `circles`

- 兴趣 / 地区 / 行业三类子社群，加入、社群活动列表、部分成员、在社群里办活动、创建社群。
- `events.circleId` 把活动挂到社群。
- **扩展点**：社群公告、社群管理员、成员列表真实化。

## 创业 `startup`

- 校内资源指南（Tsai CITY、Yale Ventures 等，含核实状态）、项目 & 找合伙人、"我能提供 / 我需要"匹配（基于 `users.skills` 与 `projects.needs`）。
- **扩展点**：投资人对接流程、项目进展更新。

## 生活指南 `life`

- 分类 × 地区 × 搜索，条目带维护人、核实状态、"有用"计数；经验帖；贡献条目。
- **扩展点**：版本历史、过期提醒。

## 校友目录 `directory`（需登录）

- 搜索 + 可提供帮助 / 行业 / 地区筛选；个人主页（简介、标签、帖子、组织的活动、贡献值、徽章）；打招呼、约咖啡。
- **扩展点**：站内私信、联系方式交换确认。

## 学联后台 `acssy`（需学联身份）

| Tab | 内容 |
|---|---|
| 活动项目 | 统计（进行中 / 我的待办 / 待认领 / SOP 数）、我的待办、项目卡片（进度条、待认领数）。项目详情：按阶段的任务列表，状态切换、认领、分配（负责人）、反馈进度（带历史）、添加任务；项目通知；志愿者岗位；SOP 关键重点 |
| SOP 库 | 每份 SOP：规模、提前量、摘要、关键重点、阶段与任务（含部门）、材料清单、关联模板与联系人、历史记录；"用这份 SOP 办活动" |
| 联系人 | 场地 / 赞助 / 供应商 / 学校部门 / 合作组织 / 讲者，标签、渠道、维护人、最近联系、评分、备注；"我也认识"；添加联系人 |
| 沟通模板 | 邮件 / 微信推送 / 通知 / 表单，点开复制 |
| 志愿者 | 进行中活动的岗位、时段、已报 / 需要，报名 |

### "我想办活动"向导（`YL.acssy.openWizard`）

1. 选类型：社群活动（轻量）或学联官方活动（需学联身份）；
2. 学联活动选 SOP，预览任务数与关键重点；
3. 填活动信息（可关联社群）。
4. 提交：创建活动并自动报名；学联活动同时创建项目，任务与截止日期由 SOP 的 `days` 偏移量按活动日期生成，跳转到看板。

- **扩展点**：任务提醒与逾期通知、部门视图、预算表、活动后复盘自动回写 SOP。

## 个人主页 `profile`（需登录）

- 资料、学联身份标识、我的帖子 / 活动 / 小组 / 收藏岗位、贡献值与徽章、清空演示数据。

## 关于 & 共创 `about`

- 愿景、模块列表（自动）、参与方式、路线图、信任边界、免责声明。

## 注册表 API

```js
registerModule({
  id, nav: { icon, labelKey, order, mobile, when }, requiresAuth, descriptionKey, render(root, ctx)
});
```

- `nav.when()` 返回 false 时不显示（学联后台用它只对成员显示）；
- `ctx = { path, segments, module, sub, id, query }`；
- 未登录访问 `requiresAuth` 模块会跳转到登录并在登录后返回。
